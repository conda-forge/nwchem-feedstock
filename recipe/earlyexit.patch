--- a/QA/doafewqmtests.mpi
+++ b/QA/doafewqmtests.mpi
@@ -8,6 +8,11 @@
 let np=1
 if [ -n "$1" ]; then
    np="$1"
+fi
+if [ -n "$2" ]; then
+    #   early exit
+    echo early exit
+    set -e
 fi
   echo
   echo " ======================================================="
